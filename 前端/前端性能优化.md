## 开发时的构建速度优化
yarn dev / npm run dev 执行后到呈现结果用了多长时间

- webpack 在这方面做了很多：cache-loader， cache， loader结果（如果两次构建源代码没有发生变化，则直接使用缓存 不调用loader）
- vite是按需加载，不需要考虑

## 页面性能指标（和我们怎么写代码有关）
- 首屏渲染时：fcp（first content paint）页面第一个元素的渲染时长
	- 懒加载：需要写代码实现
	- http优化：协商缓存和强缓存
		- 强缓存：服务端给响应头追加一些字段（expires），客户端会